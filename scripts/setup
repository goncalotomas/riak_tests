echo "===> Running pre_ct hook"
echo "Here I am supposed to copy the Riak release artifacts to a folder that is accessible from within CT suites"
echo "Also delete and create (to allow multiple executions) data directories for Riak nodes within the CT suites"
